# Firebase App Hosting configuration for DIBF
# Environment variables are managed in the Firebase Console.

runConfig:
  minInstances: 0
  maxInstances: 10
  concurrency: 80
  cpu: 1
  memoryMiB: 512
